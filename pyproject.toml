[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]

[tool.mypy]
files = ["backend"]
python_version = "3.12"
strict = true
ignore_missing_imports = true

[tool.ruff]
src = ["backend"]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
